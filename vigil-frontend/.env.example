# Frontend dev server port
VITE_DEV_PORT=5173

# Backend API base target used by Vite proxy in development
# Standard setup: backend on 8000
VITE_API_TARGET=http://localhost:8000

# Set true only when developing without backend
VITE_USE_MOCK=false
